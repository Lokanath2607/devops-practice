dockerfiles/README.md
# Dockerfiles
This folder contains Dockerfiles for sample applications.

- sample-app.Dockerfile: Node.js app container
- python-app.Dockerfile: Python Flask app container
- nginx.Dockerfile: Nginx web server container
